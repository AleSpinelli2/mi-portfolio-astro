---
const { class: className, id } = Astro.props;
---

<section
  id={id}
  data-section={id}
  class={`section`}
  >
  <!-- class={`section ${className} scroll-m-20 w-auto mx-auto container lg:max-w-6xl md:max-w-2xl`} -->

  <slot />
</section>

<style>
</style>
